OK so I don’t know what to talk about in the report so here’s just my thoughts on the tasks we had to do.

Task 1
This was very intuitive and easy to write because there are a lot of similarities to Java and I already had an idea of what pointer was. Basically the code just assigns a variable's memory address to a pointer and accesses the value directly through it.
Task 2
The hardest part here was figuring out how to write an array in C, and then the text of the output so it didn't sound like an AI generated :). I learned that we can change the values of variables solely using pointers which is something I had never done before. The code walks through the array and uses a pointer to directly overwrite the numbers in memory.
Task 3
This took me some time to figure out and then optimize. I also didn't know that a function’s location in the file mattered, because that’s definitely not how Java works! This was a fun and unique challenge for me, as the primitive type worked almost like an object would in Java. To make it work the code passes the variable's memory address into the function so it can modify the original data directly instead of just editing a temporary copy.
Task 4
Double pointers were painful, I have to admit. I had a hard time understanding the meaning behind them and the way they worked before I could actually start writing. But as soon as I forced myself to think like a computer would, it became exponentially easier. The code basically sets up a pointer that points to the address of another pointer, which lets me modify the original reference dynamically. Also naming variables and pointers is a tough thing for me, so sorry for any incomprehensible naming in my source code.
Task 5
This is where I really gained the most knowledge from this homework. I had never worked with an array of chars or seen how a while statement and pointers work together in C. The code essentially uses a while looop to step a pointer through a char array moving one letter at a time until it hits the null terminator at the end.


